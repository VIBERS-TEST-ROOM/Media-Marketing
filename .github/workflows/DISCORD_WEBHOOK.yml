name: Discord General Updates

on:
  issues:
    types: [opened, closed]
  pull_request:
    types: [opened, closed]

jobs:
  notify-general:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        if: ${{ !startsWith(github.event.issue.title, '[Bounty]') && !startsWith(github.event.issue.title, '[Quest]') }}
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_USERNAME: Github Notfis Bot
          DISCORD_AVATAR: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png
          DISCORD_CONTENT: |
            🏛️ **Media and Marketing WG Update**
            📂 Repo: ${{ github.repository }}
            👤 By: ${{ github.actor }}
            📌 Event: ${{ github.event_name }}

            ${{ github.event.issue.title || github.event.pull_request.title || 'No title' }}
            🔗 [View on GitHub](${{ github.event.issue.html_url || github.event.pull_request.html_url || github.event.comment.html_url || github.event.head_commit.url }})
